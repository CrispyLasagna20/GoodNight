using UnityEngine;

public class AnamolyAssertion : MonoBehaviour
{
    //list of anomalies
    public GameObject pictureAnomaly;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //pictureAnomaly.transform.position = new Vector3(0, 0, 0);
    }
}
